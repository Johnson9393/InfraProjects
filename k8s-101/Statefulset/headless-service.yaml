apiVersion: v1
kind: Service
metadata:
  name: stateful-demo
spec:
  clusterIP: None
  selector:
    app: stateful-demo # Find pods with this label and send traffic to them
  ports:
    - port: 80
      targetPort: 80