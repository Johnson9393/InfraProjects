## Pytho Automation use cases

* Use Case 1 : Delete the access that are older than 2 days for all list of IAM users
* Use Case 2: When Vendor uploads a large transactional data from UI, it should upload to S3 then Lambda triggers and download it and write to the RDS

